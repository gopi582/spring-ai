spring:
  application:
    name: api-gateway-service
  cloud:
    gateway:
      routes:

        - id: ai-agent
          uri: http://localhost:8081
          predicates:
            - Path=/ai/**

        - id: auth
          uri: http://localhost:8083
          predicates:
            - Path=/auth/**
    
server:
  port: 8080
